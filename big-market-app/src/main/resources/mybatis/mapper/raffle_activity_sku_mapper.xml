<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.c.infrastructure.dao.IRaffleActivitySkuDao">

    <resultMap id="dataMap" type="com.c.infrastructure.po.RaffleActivitySku">
        <id column="id" property="id"/>
        <result column="sku" property="sku"/>
        <result column="activity_id" property="activityId"/>
        <result column="activity_count_id" property="activityCountId"/>
        <result column="stock_count" property="stockCount"/>
        <result column="stock_count_surplus" property="stockCountSurplus"/>
        <result column="product_amount" property="productAmount"/>
        <result column="state" property="state"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryActivitySku" parameterType="java.lang.Long" resultMap="dataMap">
        SELECT id,
               sku,
               activity_id,
               activity_count_id,
               stock_count,
               stock_count_surplus,
               product_amount,
               state,
               create_time,
               update_time
        FROM raffle_activity_sku
        WHERE sku = #{sku}
          AND state = 0
    </select>

    <select id="queryActivitySkuListByActivityId" resultMap="dataMap" parameterType="java.lang.Long">
        SELECT id,
               sku,
               activity_id,
               activity_count_id,
               stock_count,
               stock_count_surplus,
               product_amount,
               state,
               create_time,
               update_time
        FROM raffle_activity_sku
        WHERE activity_id = #{activityId}
          AND state = 0
    </select>

    <update id="updateActivitySkuStock" parameterType="java.lang.Long">
        UPDATE raffle_activity_sku
        SET stock_count_surplus = stock_count_surplus - 1,
            update_time         = NOW()
        WHERE sku = #{sku}
          AND stock_count_surplus > 0
          AND state = 0
    </update>

    <update id="clearActivitySkuStock" parameterType="java.lang.Long">
        UPDATE raffle_activity_sku
        SET stock_count_surplus = 0,
            update_time         = NOW()
        WHERE sku = #{sku}
    </update>

    <update id="updateActivitySkuStockCount" parameterType="map">
        UPDATE raffle_activity_sku
        SET stock_count_surplus = stock_count_surplus - #{count},
            update_time         = NOW()
        WHERE sku = #{sku}
          AND stock_count_surplus >= #{count}
          AND state = 0
    </update>

</mapper>